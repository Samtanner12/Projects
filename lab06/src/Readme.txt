the root node is used for traversal
the root is set to max element in the heap
and then the root and the weight is printed by calling the root key
then the totl letters are printed in the summary
and the average code length takes the sum of the wighted path and divides by the root key
the current node is then set to the root